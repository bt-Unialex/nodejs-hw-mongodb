description: Not found error
content:
  application/json:
    schema:
      type: object
      properties:
        status:
          type: integer
          example: 404
        message:
          type: string
          example: 'NotFoundError'
        data:
          type: object
          properties:
            message:
              type: string
              example: 'Things not found'
